# Host targets live in ~/.ssh/config.d/*.
# Keep Host * last so host-specific entries can override these defaults.
Include ~/.ssh/config.d/*

Host *
    ControlMaster no
    ServerAliveInterval 60
    ServerAliveCountMax 3
    IdentityFile ~/.ssh/id_ed25519
    IdentitiesOnly yes
